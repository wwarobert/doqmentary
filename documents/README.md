# Generated documents

Solution outlines scaffolded by `doqmentary new` live here, one folder per
solution (for example `documents/<solution>/`). Each document folder may contain
its own `doqmentary.yaml` to override the global defaults.

This file keeps the folder tracked while it is otherwise empty.
